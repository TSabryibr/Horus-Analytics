{
    "name": "horus-analytics-monorepo",
    "private": true,
    "workspaces": [
        "frontend"
    ],
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "build": "npm run build --prefix frontend",
        "test": "npm test --prefix frontend",
        "test:backend": "python scripts/run_backend_tests.py",
        "test:backend:collect": "python scripts/run_backend_tests.py --collect-only",
        "cleanup:backups": "python scripts/cleanup_runtime_backups.py"
    },
    "overrides": {
        "postcss": "^8.5.10",
        "next": {
            "postcss": "^8.5.10"
        }
    }
}
